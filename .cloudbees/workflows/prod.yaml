apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: prod
on:
  workflow_dispatch:

jobs:
  deploy:
    steps:
      - uses: cloudbees-io/register-deployed-artifact@v2
        with:
          labels: SVMultiLangRepo
          artifact-id: bc67851f-5a80-4cdb-ba86-048939e79071
          target-environment: ASPM_QA_ENV